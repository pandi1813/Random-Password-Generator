// Prompt the user to enter how many characters they want the password to be
    // store returned value in a variable - how?

//use confirmations to ask which character types they want to use
    // concat arrays that return true

    // look up how to use confirm

// creating password
    //use the new concat array to select characters from

    //create random number to use as index numbers 

    // for loop to select the required number of characters
        //how many times to run it = the return value from prompt